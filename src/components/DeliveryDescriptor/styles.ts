import { StyleSheet } from "react-native";

export const layoutStyles = StyleSheet.create({
  row: { flexDirection: "row", width: "100" },
  labelContainer: { padding: 2, borderColor: "#ccc", borderRadius: 20, borderWidth: 2 },
});
